# =============================================================================
# tgbotstory2 — Python dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# Minimum Python: 3.10+
# =============================================================================

# --- Telegram Bot Framework ---
aiogram>=3.7,<4.0

# --- VK API ---
# vkbottle — async VK API framework with type hints
vkbottle>=4.3,<5.0

# --- Instagram API ---
# instagrapi — unofficial Instagram Private API client
instagrapi>=2.1,<3.0

# --- TikTok API ---
# TikTokApi — unofficial TikTok API (async, uses Playwright)
TikTokApi>=6.2,<7.0

# --- Browser Automation (for TikTok) ---
playwright>=1.44,<2.0

# --- Database ---
# SQLAlchemy 2.0+ with async support
sqlalchemy[asyncio]>=2.0,<3.0
aiosqlite>=0.20,<1.0

# --- Task Scheduling ---
# APScheduler — in-process async job scheduler
apscheduler>=3.10,<4.0

# --- Configuration ---
# pydantic-settings — .env file loader with validation
pydantic-settings>=2.2,<3.0

# --- Logging ---
# structlog — structured logging (key=value pairs)
structlog>=23.2,<27.0

# --- Encryption ---
# cryptography — Fernet symmetric encryption for credential storage
cryptography>=41.0,<44.0

# --- HTTP Clients ---
# httpx — modern async HTTP client (used by vkbottle, aiogram)
httpx>=0.27,<1.0
# aiohttp — async HTTP client/server (used by instagrapi, TikTokApi)
aiohttp>=3.9,<4.0

# --- Testing ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
